
import { redirect } from 'next/navigation';

export default function Home() {
  // Redirigimos al dashboard que está dentro del grupo (dashboard)
  // En Next.js, (dashboard)/page.tsx ya resuelve a /
  // Pero mantenemos este archivo para evitar confusiones de estructura.
  return null;
}
